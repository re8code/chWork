[1 Page]
- *this + pt2 -> : 이 주석의 의미는 ? 특히 화살표(->)의 의미는 ?

[2 Page]
- SpyPoint class 의 용도는?
- 구체적으로 어떤 메소드를 구성해야 하는가?
- x, y, numCreateObj : 이 멤버들은 Point 객체에 있는 정보 ?

[3 Page]
- pPt1 포인터는 정적 할당된 pt1에 대한 주소 변수이기에 동적 메모리 해제가 불가능한데, 해제를 시킬 수 있나?
- 만약, pPt2 동적 포인터만을 해제한다면, [4 Page]의 실행 결과 화면에서 소멸자 호출이 1번만 발생되는데,
  이 부분은 어떻게 구성되어야 하는가?

- pt2, pt3, pt4 객체의 생성은 [4 Page] 실행 결과 화면에 따르면, (10,20), (30,40), 이들의 합 형태로
  생성시키도록 구현하면 되는 것인가?
- NumCreatedObject 출력 과정에서 "pt1"을 사용하고 있는데, 정적 할당된 pt1과의 관계성이 있어야 하나?

- object array 파트에서 Point 객체의 배열을 생성해야 하는데, 출력 결과에 따르면 5개로 구성된 듯 한데, 맞는가?
- 이 과정에서 NumCreatedObject 출력을 보면 "pt2"을 사용하고 있는데,
  바로 위에서 구현 정의된 pt2와의 관계성이 있어야 하나?
